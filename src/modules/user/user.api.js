
const PAGE_LENGTH = 20

export default {
    pageLength: PAGE_LENGTH,
    save(user) {
        //send (create/update) user to api
    },
    delete(userId) {
        //delete user by id
    },
    query(seach) {
        // return ids in page 0 by query
    }
}